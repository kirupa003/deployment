# GitOps Synchronization Report

**Generated:** {{ sync_timestamp }}

## Repository Information

- **Repository:** {{ git_repo_url | default('Unknown') }}
- **Branch:** {{ git_branch | default('Unknown') }}
- **Commit:** {{ git_commit | default('Unknown') }}
- **Author:** {{ git_author | default('Unknown') }}
- **Message:** {{ git_message | default('Unknown') }}

## Synchronization Details

- **Target Hosts:** {{ target_hosts }}
- **Sync Required:** {{ 'Yes' if sync_needed else 'No' }}
- **Changes Applied:** {{ 'Yes' if changes_applied else 'No' }}
- **Execution Time:** {{ sync_timestamp }}

## Summary

{% if sync_needed %}
✅ **Configuration changes detected and applied successfully**

The following actions were performed:
- Configuration files synchronized from Git repository
- Services restarted where necessary
- Health checks validated service functionality
- All target hosts updated successfully

{% else %}
ℹ️ **No configuration changes required**

The current configuration matches the Git repository state.
No synchronization actions were needed.

{% endif %}

## Next Steps

{% if sync_needed %}
1. Monitor service health and performance
2. Verify client connectivity and functionality
3. Review logs for any issues
4. Update monitoring dashboards if needed

{% else %}
1. Continue regular monitoring
2. No immediate action required

{% endif %}

---
*Report generated by GitOps synchronization playbook*